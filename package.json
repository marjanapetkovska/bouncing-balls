{
  "name": "bouncingballs_project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "babel": "babel js/main.js --presets @babel/preset-env -o build/main.js",
    "browserify": "browserify js/main.js --presets @babel/preset-env -o build/main.js",
    "start": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.8.7",
    "@babel/preset-env": "^7.8.7",
    "@babel/preset-es2015": "^7.0.0-beta.53",
    "babelify": "^10.0.0",
    "browserify": "^16.5.0"
  },
  "dependencies": {
    "@babel/polyfill": "^7.8.7",
    "http-server": "^0.12.1"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "@babel/preset-env"
          ]
        }
      ]
    ]
  }
}
